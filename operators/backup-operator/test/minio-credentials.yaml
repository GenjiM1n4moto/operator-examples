---
# MinIO credentials Secret for backup-operator
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: default
type: Opaque
stringData:
  access-key: "minioadmin"
  secret-key: "minioadmin123"
  endpoint: "http://minio.minio.svc.cluster.local:9000"
  region: "us-east-1"
  restic-password: "backup-password-123"
