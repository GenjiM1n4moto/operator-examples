---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backuppolicies.backup.backup.example.com
spec:
  group: backup.backup.example.com
  names:
    kind: BackupPolicy
    listKind: BackupPolicyList
    plural: backuppolicies
    singular: backuppolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.strategy
      name: Strategy
      type: string
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lastBackupTime
      name: Last Backup
      type: date
    - jsonPath: .status.backupCount
      name: Backups
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupPolicy is the Schema for the backuppolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupPolicySpec defines the desired state of BackupPolicy.
            properties:
              destination:
                description: Destination for external backups (required when strategy=external)
                properties:
                  credentialsSecret:
                    description: Secret name containing credentials for accessing
                      the destination
                    type: string
                  endpoint:
                    description: |-
                      Custom endpoint for S3-compatible storage (e.g., MinIO)
                      Examples:
                        MinIO: http://minio.minio.svc.cluster.local:9000
                        Ceph: http://ceph-rgw.ceph.svc:8080
                    type: string
                  storageClass:
                    description: Storage class for S3-compatible backends (STANDARD,
                      GLACIER, DEEP_ARCHIVE)
                    type: string
                  type:
                    description: 'Backup destination type: s3, nfs, gcs, azure'
                    enum:
                    - s3
                    - nfs
                    - gcs
                    - azure
                    type: string
                  url:
                    description: |-
                      Destination URL or endpoint
                      Examples:
                        S3: s3://bucket-name/prefix
                        NFS: nfs://server-address/export/path
                        GCS: gs://bucket-name/prefix
                    type: string
                type: object
              namespaces:
                description: Namespaces to search for PVCs (empty means all namespaces
                  if RBAC permits)
                items:
                  type: string
                type: array
              restore:
                description: Restore configuration (optional, for future restore operations)
                properties:
                  namespace:
                    type: string
                  selector:
                    description: |-
                      A label selector is a label query over a set of resources. The result of matchLabels and
                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                      label selector matches no objects.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              retention:
                description: Retention policy for backup cleanup
                properties:
                  maxAge:
                    type: string
                  maxBackups:
                    type: integer
                type: object
              schedule:
                description: Cron schedule for backups (e.g., "0 2 * * *" for daily
                  at 2 AM)
                type: string
              selector:
                description: Label selector for PVCs to backup
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              strategy:
                default: snapshot
                description: |-
                  Backup strategy: "snapshot" (VolumeSnapshot) or "external" (S3/NFS)
                  snapshot: Fast, local, short-term (default)
                  external: Slower, remote, long-term
                enum:
                - snapshot
                - external
                type: string
            required:
            - schedule
            type: object
          status:
            description: BackupPolicyStatus defines the observed state of BackupPolicy.
            properties:
              backupCount:
                description: Total number of backups currently stored
                type: integer
              conditions:
                description: Standard condition types for status reporting
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBackupTime:
                description: Timestamp of the last successful backup
                format: date-time
                type: string
              nextRunTime:
                description: Calculated next run time based on schedule
                format: date-time
                type: string
              phase:
                description: 'Current phase: Active, Error, Suspended'
                type: string
              storedBackups:
                description: List of stored backups with metadata
                items:
                  properties:
                    location:
                      description: |-
                        Full location/path of the backup
                        Examples:
                          Snapshot: default/pvc-snapshot-xyz
                          S3: s3://bucket/backups/mysql-20250103-020000.tar.gz
                      type: string
                    name:
                      description: Backup name/identifier
                      type: string
                    namespace:
                      description: Source PVC namespace
                      type: string
                    pvcName:
                      description: Source PVC name
                      type: string
                    size:
                      description: Backup size (human-readable, e.g., "1.5Gi")
                      type: string
                    status:
                      description: 'Backup status: Completed, Failed, InProgress'
                      type: string
                    strategy:
                      description: 'Backup strategy used: snapshot, external'
                      type: string
                    timestamp:
                      description: When this backup was created
                      format: date-time
                      type: string
                  required:
                  - location
                  - name
                  - namespace
                  - pvcName
                  - status
                  - timestamp
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
